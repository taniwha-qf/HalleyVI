INTERNAL {
	name = hrs-chassis-end-internal
	MODEL {
		model = HalleyVI/hrs-chassis-end-internal
		position    =   0.0, 0.0, 0.0
		rotation    =   0.0, 0.0, 0.0
		scale       =   1.0, 1.0, 1.0
	}
	MODULE {
		name = DIVA_InternalModelSwitch
		SwitchInfo {
			node = top
			transform = toggle-panel.fore
			mode = DisableWhenConnected
		}
		SwitchInfo {
			node = bottom
			transform = toggle-panel.aft
			mode = DisableWhenConnected
		}
		SwitchInfo {
			node = top01
			transform = toggle-panel.top
			mode = DisableWhenConnected
		}
	}
}
